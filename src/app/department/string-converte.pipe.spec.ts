import { StringConvertePipe } from './string-converte.pipe';

describe('StringConvertePipe', () => {
  it('create an instance', () => {
    const pipe = new StringConvertePipe();
    expect(pipe).toBeTruthy();
  });
});
